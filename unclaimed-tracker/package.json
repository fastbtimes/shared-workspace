{
  "name": "claimtrail",
  "version": "1.0.0",
  "private": true,
  "description": "ClaimTrail — unclaimed-property tracker (static front-end + Stripe Checkout serverless function).",
  "scripts": {
    "dev": "python3 -m http.server 8000"
  },
  "dependencies": {
    "stripe": "^16.12.0"
  }
}
